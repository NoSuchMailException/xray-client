# Proxy Client

A lightweight, modular console proxy client written in Go utilizing **VLESS + REALITY** over a **gRPC** transport layer.

## Installation

Download the pre-compiled binary for your operating system from the [Releases](../../releases) page.

## Usage
Run:
```bash
./proxy-client
```

Run with debug logging enabled:
```bash
./proxy-client -v
```

---